\ifx\exebook\mytoken \documentclass{mmcm}\else \fi


\docbegin


\section{Systems with one degree of freedom}

\begin{Exec}
  Draw the phase curves for the ``equation of a pendulum one a rotating axis'':
  $\bxdd = -\sin \bx + M$
\end{Exec}

\begin{Exec}
  Find the tangent lines to the branches of the critical level 
  corresponding to maximal potential energy $E = U(\xi)$ 
\end{Exec}

\begin{solution}
  由于$dx/dt = y$, $dy/dt = -\sin \bx + M$.
  $dy/dx = (-\sin\bx+M)/y$. 解常微分方程得.
  $y^2/2 = E + \cos\bx + M\bx$. 所以相图如下(M = 0.15).
  
  \includegraphics[width=300pt]{che2-1.jpg}
\end{solution}

\begin{solution}
  By $U(x) + y^2/2 = E$, $dy/dx = -U'(x)/y$ (Since $E=U(\xi)>U(x)$,$y\neq 0$).
  By L'Hospital's Rule 
  $$\lim_{x\rightarrow \xi} dy/dx = -U''(x)/y'$$.
  So $y'(\xi) = \pm \sqrt{-U''(\xi)}$.
  Then tangent lines are $y = \pm \sqrt{-U''(\xi)}(x-\xi)$.
  ps\:我的做法是对$U(x) + y^2/2 = E$求两次导数。

\end{solution}

\begin{Exec}
  Let $S(E)$ be the area enclosed by the closed phase curve coresponding
  to the energy level $E$. Show that the period of mothion along this curve 
  is equal to $$T = \frac{dS}{dE}$$.
\end{Exec}

\begin{proof}
  由唯一性定理, 首先知道这样的相曲线与$x$轴只能有两个交点设为$x_1<x_2$
  (否则如果有一个中间点, 那么相曲线必然在那边断开了,建议画草图看一下).
  另外可知对每个半平面(上下), $x$到$y$是一一对应的
  (否则, 有一个转折点,于是就有$\bxd=0$,这于$y=\bxd\neq0$即在上, 下半平面内矛盾).
  它们与能级$E$有关.
  考虑相曲线围成的面积
  $$S = \int_{x_1}^{x_2} y dx + \int_{x_2}^{x_1} y dx .$$
  (注意后面一项的积分方向).
  结合$y = \pm \sqrt{2(E-U)}$.
  所以
  $$dS/dE = \int_{x_1}^{x_2} \frac{dx}{\sqrt{2(E-U)}}
  + \int_{x_1}^{x_2} \frac{dx}{\sqrt{2(E-U)}}
  + y dx/dE \vat_{x_1}^{x_2} + y dx/dE \vat_{x_2}^{x_1}
  $$
  注意到$y$在$x_1,x_2$处取零, 上式第一项就是运动从$x_1$到$x_2$的时间,
  后一项是$x_2$到$x_1$的时间, 和即为一个周期的运动时间. 所以$T = dS/dE$.

  ps\:由公式$U(x) + y^2/2 =E$可以看到相曲线是关于$x$轴对称的，\
  所以$T/2=\int_{x_1}^{x_2} \frac{dx}{\sqrt{2(E-U)}}$由Green公式，
    $S /2= \int_{x_1}^{x_2} y dx $求导即得结论。

\end{proof}

\begin{Exec}
  Let $E_0$ be the value of the potential function at a minimum point $\xi$.
  Find the period $T_0 = \lim_{E\rightarrow E_0} T(E)$ of small oscillations in 
  a neighborhood of the point $\xi$.
\end{Exec}

\begin{Exec}
  Consider a periodic motion along the closed phase curve
  corresponding to the energy level $E$.
  Is it stable in the sense of Liapunov?
\end{Exec}

\begin{solution}
  The Definition of Liapunove stability:
  (By Nonlinear Ordinary Differential Equations:
  nonlin Ordinary Diff Equat 3e P286)

  Let $\bx^\ast(t)$ be a given real or complex solution vector 
  of the $n$-dimentional system $\bxd = X(\bx,t)$. Then 
  $\bx^\ast$ is Liapunov stable for $t\geq t_0$ if and only if, 
  to each value of $\varepsilon>0$, however small, there corresponds
  a value of $\delta>0$ (where $\delta$ may depend only on $\varepsilon$
  and $t_0$) such that
  $$\|\bx(t_0) - \bx^\ast(t_0)\|<\delta 
  \Rightarrow \|\bx(t) - \bx^\ast(t)\|<\varepsilon$$
  for all $t>t_0$, where $\bx(t)$ represents any other neighbouring solution.

  Example:
  
\end{solution}

\begin{Exec}
  Draw the imaage of the circle $x^2+(y-1)^2<\frac{1}{4}$ under the action 
  of a transformation of the phase flow for the equations (a) of the 
  ``inverse pendulum'', $\ddot{x} = x$ and (b) of the ``nonlinear pendulum'',
  $\ddot{x} = -\sin x$.
\end{Exec}

\begin{solution}
  做首次积分, 由$\ddot{x} = x$ 得$dy/dx = x/y$, 所以$y^2-x^2 = E$.
  考虑一支$\dot{x} = y = \sqrt{E+x^2}$. 解得$x = \sqrt{E} \sinh{(t+C)}$,
  于是$y=\dot{x} = \sqrt{E} \cosh{(t+C)}$.
  其中$E = y^2-x^2$, $C = \arcsinh{(x/\sqrt{E})}$.
  于是得到相流$g^t(x,y) = (\sqrt{E} \sinh{(t+C)},\sqrt{E}\cosh{(t+C)})$.
  如图:

  \includegraphics[width=300pt]{che2-3.jpg}

  (b) 同样先首次积分,
  用数值解法, 算出了图如下:
  
  \includegraphics[width=300pt]{che2-4.jpg}
  
\end{solution}


\section{Systems with two degrees of freedom}

\docend